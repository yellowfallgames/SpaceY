class meteorito extends Phaser.GameObjects.Sprite{
    constructor(angle, velocity, scene){
        //var obj;

        this.angle = angle;
        this.velocity = velocity;
        ////this.physics.add.overlap(player, terraformador, colliderInteract);
        //super(scene, marte.x, marte.y, "marte");
        //this.setScale(0.25);

        //this.obj = scene.add.image(marte.x, marte.y, "marte");
        //this.obj.setSizeX(0.25);

        //this.obj.setOrigin(0.5, 9.5);
    }
    /*// Getters
    getAngle(){
        return this.angle;
    }

    getVelocity(){
        return this.velocity;
    }

    // Setters
    setAngle(value){
        this.angle = value;
    }

    setVelocity(value){
        this.velocity = value;
    }*/

    //
    
}

export default meteorito;